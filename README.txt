HQL v73 CLEAN REBUILD

Upload/replace these exact files in the folder GitHub Pages publishes from:
- index.html
- styles.css
- script.js
- projects.js

Important: delete or ignore files named index(2).html, styles(1).css, script(1).js, projects(1).js.
The browser references the unsuffixed names above.

What was cleaned:
- One Contact button in the HTML, not separate desktop/mobile buttons. Its label changes by CSS.
- Inter 300 is now actually loaded, so the contact biography can render at a real light weight.
- All accumulated v64-v72 CSS override blocks were removed.
- One mobile menu implementation remains: fixed 100vw x 100dvh, no clip-path, no filtered/transformed ancestor.
- Mobile homepage contact details remain under the name.
- WORK / selected category remains above the project grid.
- Mobile project and Contact close controls are black squares with centered white crosses.
- Existing project data, credits, media, photo series, BTS rail, video-series previews and ordering are preserved.

Version marker: HQL VERSION 73 CLEAN REBUILD
