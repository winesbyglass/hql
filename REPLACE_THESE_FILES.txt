Replace these three files together:
1. index.html
2. script.js
3. styles.css

projects.js is unchanged.

v71 is a focused bug fix:
- Desktop restores the normal 'Contact' control; CONTACT / BIO is mobile-menu-only.
- Contact biography text is explicitly 300 weight with neutral tracking.
- Mobile three-line menu no longer uses clip-path or a filtered/sticky ancestor that can constrain the fixed sheet.
- The mobile menu is forced to 100vw × 100dvh and slides from -100% to 0, so it opens fully.
- CONTACT / BIO remains at the bottom of the mobile menu and opens the existing Contact/Bio page.
